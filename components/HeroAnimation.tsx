'use client';

import React from 'react';
import { CanvasHeroAnimation } from '@/components/CanvasGraphics';

export default function HeroAnimation({ displayPrices = [] }: { displayPrices?: any[] }) {
    return <CanvasHeroAnimation />;
}
